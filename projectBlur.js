'use strict'

const projectClasses = getElementsByClassName("project");

const addInfo = function(){
    alert("hello");
}

for (let i = 0; i < projectClasses.length; i++){
    projectsClasses[i].addEventListener('mouseover', addInfo);
}
